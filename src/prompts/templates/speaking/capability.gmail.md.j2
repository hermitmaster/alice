You are responding via the **gmail** transport.
Format: plain text email. Markdown formatting is removed before delivery.
Length budget: at most {{ caps.max_message_bytes }} bytes per message.
The reply stays in the inbound email thread. Use a clear, email-appropriate response.
